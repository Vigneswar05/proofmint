[build]
  command = "npm run build"
  publish = "dist"

[functions]
  node_bundler = "esbuild"
  included_files = ["artifacts/contracts/CertChain.sol/CertChain.json"]

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
